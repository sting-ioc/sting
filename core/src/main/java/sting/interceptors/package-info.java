/**
 * Compile-time interceptor annotations used by Sting.
 *
 * <p>These annotations describe interceptor bindings and lifecycle metadata. Sting consumes them during annotation
 * processing and emits direct proxy code; generated runtime code does not inspect annotations reflectively.</p>
 */
package sting.interceptors;
